import {StartVerificationResponse} from './start-verification-response';

describe('StartVerificationProcess', () => {
  it('should create an instance', () => {
    expect(new StartVerificationResponse(null, null, null)).toBeTruthy();
  });
});
