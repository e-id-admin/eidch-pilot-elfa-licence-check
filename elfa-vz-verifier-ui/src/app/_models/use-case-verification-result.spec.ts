import {UseCaseVerificationResult} from './use-case-verification-result';

describe('UseCaseVerificationResult', () => {
  it('should create an instance', () => {
    expect(new UseCaseVerificationResult(null, null)).toBeTruthy();
  });
});
